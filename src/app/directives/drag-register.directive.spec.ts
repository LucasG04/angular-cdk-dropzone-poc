import { DragRegisterDirective } from './drag-register.directive';

describe('DragRegisterDirective', () => {
  it('should create an instance', () => {
    const directive = new DragRegisterDirective();
    expect(directive).toBeTruthy();
  });
});
