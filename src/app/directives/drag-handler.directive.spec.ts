import { DragHandlerDirective } from './drag-handler.directive';

describe('DragHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new DragHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
